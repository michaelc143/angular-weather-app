import { DatePipe } from '@angular/common';
import { Component } from '@angular/core';


@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {

  title = 'weather-app'; //this is used in the is running part next to the spaceship
  //currentDate = 'May 29, 2023';  // Represents a specific date in a custom string format
}
